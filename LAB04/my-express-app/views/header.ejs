<header>
    <nav>
      <ul>
        <% navLinks.forEach(function(link) { %>
          <li><a href="<%= link.href %>"><%= link.text %></a></li>
        <% }); %>
      </ul>
    </nav>
  </header>
  